apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: userdir
spec:
  selector: { app: frontend }
  ports:
    - name: http
      port: 5173
      targetPort: 5173
  # For dev, keep ClusterIP and use port-forward:
  # type: NodePort
  # nodePort: 30080