apiVersion: v1
kind: Service
metadata:
  name: {{ include "userdir.fullname" . }}-postgres
spec:
  selector: { app: {{ include "userdir.fullname" . }}-postgres }
  ports:
    - port: {{ .Values.postgres.service.port }}
      targetPort: {{ .Values.postgres.service.port }}
      name: pg